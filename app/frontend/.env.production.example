# Optional — only set if the frontend talks to the API directly (HTTPS backend required).
# Default production build uses same-origin /hedge-fund/* rewrites in vercel.json → AWS ALB.
# VITE_API_URL=https://api.your-domain.example.com
