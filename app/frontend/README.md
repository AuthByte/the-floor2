# THE FLOOR — Frontend

React + Vite client for the after-hours floor UI: pixel rooms, debate theater, shift replay, weather report, and boss memo.

## Dev

```bash
cd app/frontend
npm install
npm run dev
```

Set `VITE_API_URL` if the backend is not on `http://localhost:8000`.
